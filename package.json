{
  "name": "dnms",
  "version": "0.1.3",
  "bin": {
    "dnms": "dist/main.js"
  },
  "scripts": {
    "build": "bun build main.ts --outdir=dist --minify --sourcemap=external --target=bun",
    "prepublishOnly": "bun run build",
    "publish:npm": "bun run build && npm publish"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
